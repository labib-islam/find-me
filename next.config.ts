import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",
  basePath: '/find-me', // if using GitHub Pages
  assetPrefix: '/find-me',
};

export default nextConfig;
